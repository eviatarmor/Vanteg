{
  "name": "vanteg",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "test": "npm run test -w @workspace/integrations && npm run test -w client"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "turbo": "^2.9.18",
    "typescript": "~6"
  },
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "client",
    "packages/*"
  ],
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
